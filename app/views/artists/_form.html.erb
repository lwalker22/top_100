<h1><%= @artist.id ? 'Edit Artist' : 'Create Artist' %></h1>
<hr />
<%= form_for [@billboard, @artist] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <br />
  <%= f.label :feat_artist %>
  <%= f.text_area :feat_artist, required: true %>

  <%= f.label :genre %>
  <%= f.text_area :genre, required: true %>
  <%= f.submit %>
<% end %>